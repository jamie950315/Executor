# Executor Agent Guide

## Mission

Executor is a self-hosted, cross-platform MCP server that lets an authenticated AI control the host computer with unrestricted owner and administrator capabilities. It supports macOS, Windows, Linux, and WSL.

## Approved product requirements

- Public repository designed for a coding agent to deploy after clone.
- Official product and binary name: `Executor` / `executor`.
- Remote access uses a user-owned Cloudflare account, zone, named Tunnel, and hostname.
- MCP transports: Streamable HTTP and local stdio; legacy SSE compatibility may be enabled.
- Authentication: OAuth 2.1 by default, optional explicitly unsafe URL-secret compatibility mode.
- No directory allowlist or command allowlist. Owner and administrator terminals may access the whole host.
- Persistent terminal sessions retain cwd, environment, background processes, PTY input, and output.
- Active logged-in desktop control is in v1: screenshots, accessibility tree, mouse, keyboard, windows, and applications.
- GUI control stops when the desktop is locked or logged out; terminal/admin control remains available.
- Components: unprivileged `executor-agent`, privileged `executor-broker`, active-user `executor-desktop`, CLI `executor`, and independent `executor-kill`.
- Local dashboard binds only to `127.0.0.1`.
- Kill Switch stops the tunnel and all sessions, revokes OAuth tokens, rotates URL/IPC/recovery credentials, and does not depend on the main agent being healthy.

## Engineering rules

- Use test-driven development: add and run a failing test before production behavior.
- Keep platform-specific code behind narrow interfaces and build tags.
- Never persist, log, commit, or publish secrets, tokens, recovery keys, command output, or Cloudflare credentials. The one explicit exception is delivering a newly generated recovery key directly to the requesting owner in the same private AI chat that performed the deployment or credential rotation.
- When an AI agent runs setup, Kill, rotate, or another operation that generates a recovery key on the user's behalf, it MUST reproduce the complete recovery key verbatim in its final private-chat response. It MUST NOT redact, mask, omit, or tell the user to retrieve the key from a Terminal they did not operate. Clearly label the value as sensitive and shown once, and tell the user to save it immediately. This exception applies only to the recovery key and never permits writing it to repository files, configuration, persistent logs, Issues, pull requests, or public channels.
- Secret input must use an interactive no-echo prompt or OS secret store.
- Preserve unrelated host configuration and make setup idempotent with rollback.
- README files are written in English.
- Before reporting completion, run tests, builds, cross-builds, and real local runtime checks.

## Current status

- Branch `tunnel` adds an optional official OpenAI Tunnel helper. The local
  `executor-openai-tunnel` runtime now forwards to authenticated Mac Beta HTTPS.
  Beta Agent `61aeb58` in `bundle-tunnel-csp` adds exact OAuth resource aliases
  and validated callback-origin CSP support. Credentials and production services
  are preserved. `Executor Tunnel Beta` completes OAuth and real ChatGPT AI
  create/read/overwrite/append and cross-page/binary acceptance.
  Other Beta roles keep `bundle-ec5f53d`; the dedicated stop helper validates
  these mixed paths. See `docs/OPENAI_TUNNEL.md` for evidence and rollback.

- Main now integrates the Beta-verified relay result validation, first-byte HTTP guard, persistent UI diagnostics and four-target Mac Beta maintenance helper. Installed production versions must be verified separately; merging does not deploy or rotate credentials. Beta-specific maintenance scripts remain scoped to the separate Mac Beta installation and must not manage production service labels.

- Isolated Beta Dashboard now runs UI `b5d6b29` on `codex/beta-relay-error-ui` with unchanged `b39d818` Worker code; current Worker version is `530fafe6-3806-4b9c-8ccf-36ef586238a2`. Real browser verification passes normal/empty/212 KB four-page reads, pre-first-byte HTTP 502 with persistent request-correlated error, offline Files retention and disabled controls, reconnect without retry, failed-preview Save protection, and manual dismissal. Other workspaces retain offline cleanup. D1/DO/Access remain Beta-only; enrollment stays disabled and existing Beta `bundle-ec5f53d` credentials are unchanged. Only `com.executor.beta.dashboard` was restarted for fault tests; the installed `stop.py` now validates and manages all four Beta services (canonical source `scripts/beta_stop.py`). Production was not modified. See `docs/BETA_DASHBOARD.md` for versions, acceptance and rollback.

- Beta finalization on `codex/beta-finalization` verifies the complete public HTTP 502 JSON body: 155 bytes, normal EOF, valid UTF-8/JSON, matching header/body request ID and `relay_stream_failed` / `unconfirmed`, with exactly one FIFO request. Independent bounded Dashboard-only recovery and fixture cleanup passed; normal/empty reads and unchanged credentials/production inventory passed. The real four-target stop helper was exercised only via `--check`; full stop/start is fixture-tested. Main, deployment version and Worker/UI code are unchanged. See `docs/BETA_DASHBOARD.md` for the stop helper backup, atomic rollback and acceptance procedure.

- The local relay-validation branch now guards the Dashboard HTTP first-byte boundary. Native loopback HTTP reproduced a pre-first-byte WebSocket loss as HTTP 200 with a zero-byte normal EOF; the corrected Worker returns a 155-byte JSON 502 with request correlation and an unconfirmed outcome. Five independent A/B pairs, 100 complete Beta-helper-backed 198 KB file reads through the isolated HTTP/DO test path, 301 Dashboard tests (one existing skip), checks/build and native Go/race/six-cross-build validation pass. This is a Worker/UI source change, not a deployed public Dashboard update. Production/Beta services and generation-2 Beta credentials are preserved. See `docs/2026-09-empty-http-body.md` for the exact mechanism, test-peer boundary and isolated Beta Dashboard deployment requirements.

- Mac Beta is now independently deployed from relay-validation revision `ec5f53d` at `beta-executor-mac.0ruka.dev`, using `bundle-ec5f53d`, the existing Beta credentials and `com.executor.beta.*` service labels. Production process IDs, binaries, plists, configuration and credentials remain unchanged; no Cloudflare route changes were required. Installed local stdio/helper checks pass 100 complete 210 KB Chinese reads, owner/admin files and separated terminal streams/exit 7; public metadata and unauthenticated HTTP 401 pass. Central Dashboard relay and authenticated ChatGPT acceptance remain separate coverage. Beta-scoped stop/rollback instructions are in `docs/2026-09-beta-relay-deployment.md`; this supersedes the older Beta bundle entry below.

- Local branch `fix/relay-result-validation` fixes outgoing relay result validation before both response/chunk paths and returns correlated `invalid_result` failures. TDD reproduced 24 failing subcases before the correction; full Go tests, relay race checks, 100 repeated result-regression runs, vet/native build, six CGO-disabled cross-builds, 268 Dashboard tests (one existing skip), Dashboard checks/build, and isolated local TLS/stdio runtime checks pass. The original intermittent empty HTTP body remains unconfirmed. This is local-only; existing production devices, Worker, credentials, releases, and `main` are unchanged. See `docs/2026-09-relay-result-validation.md`.

- Latest public release: `v0.3.1`, built from `185dc97` with all native Go, Dashboard/deployment and six-platform Release checks passing. GitHub hosts six verified archives plus `SHA256SUMS.txt`; the Mac archive also passed a local CLI smoke test. Automated macOS apps are ad-hoc signed/not notarized and Windows binaries are not Authenticode-signed, as stated in the release notes. `v0.3.0` remains an unpublished candidate tag and was not overwritten. Publishing did not reinstall or restart existing devices. See https://github.com/jamie950315/Executor/releases/tag/v0.3.1.

- Release validation requires the native Windows Go tests as well as the Release artifact workflow. The URL-secret integration fixture waits for authenticated Desktop IPC readiness before dispatch; successful HTTP initialization alone does not prove that the independently started helper is ready. This is a test synchronization fix, not a change to production authentication or request retry behavior.

- `main` now includes the complete `pro-fix` implementation and verification records. Use `main` for new production builds. The merge itself does not redeploy hosts or change the recorded installed revisions; `pro-fix`, temporary Beta and Fetch Proxy remain preserved.

- Authenticated Dashboard follow-up on 2026-09-14 verifies all four unlocked devices, real Terminal/Files workflows, exact 180 KB multi-page Chinese read/save, Mac automatic live video and Windows relay-only live video, real pointer/keyboard/Chinese input in isolated editors, Stop cleanup, and unlock persistence after reload. The UI-pending statement below is superseded. Initial file reads received empty relay bodies and safely disabled Save; five subsequent complete reads and exact saved-byte checks passed, but the transient cause remains unconfirmed and is not claimed fixed. No source/binary, credential, or service configuration changes were made in this follow-up.

- After deployment, refresh all four existing ChatGPT connections in Settings > Plugins and use a new conversation for schema changes. This refresh is complete for the 2026-09-14 rollout; new-conversation capability calls and Mac/Pi5/WSL pipe/stream/exit-code tests pass, with unchanged approval settings. Old conversations can retain cached schemas.

- Production Mac, Pi5, Windows CTPS, and Debian WSL run hash-verified `pro-fix` source `b6f4b64` as of 2026-09-14. Their original MCP hostnames, OAuth/recovery/IPC credentials, enrollment and TURN settings are preserved; rollback binaries are retained under each host's `deployment-backups/profix-b6f4b64-20260914`. Dashboard Worker `eb25ffbb-7c88-4c04-b72f-1d9a3556658e` serves the matching paginated file reader with unchanged Access/D1/relay bindings and disabled enrollment. Full local Go tests, focused race tests, vet/build, six cross-builds, 247 Dashboard tests (one skipped), native core tests and owner/admin runtime acceptance pass. Public OAuth identity/401 checks, existing authenticated MCP file pages, Mac/Windows screenshots and ChatGPT normal file/terminal workflows pass on all four. The fresh in-app Dashboard session requires Cloudflare sign-in, so authenticated Dashboard UI and live-video/input revalidation remain pending. See `docs/2026-09-profix-upgrade.md`; this supersedes older deployed-version entries below.

- Beta is deployed from `8dadd7e` at `https://beta-executor-mac.0ruka.dev/mcp` using `bundle-8dadd7e`, unchanged state/recovery key and `com.executor.beta.*`. Public owner/admin acceptance passes pipes/EOF, separate streams, resize 37x91 to 43x132, SIGTERM handler/file/exit 42, CWD_NOT_FOUND, screenshot and OAuth refresh. Fetch Proxy is a distinct `4d3e6e8` installation on port 19787 at `fetch-proxy-executor-mac.0ruka.dev/mcp`, with separate credentials/services. Nested-worktree Git stamping proved misleading despite correct code; standalone-checkout builds now verify the intended source revision and tool surface.

- Terminal follow-up fixes use native macOS/Linux PTYs for real resize and foreground SIGTERM delivery. Optional `terminal.create tty=false` with `argv` uses pipes; `terminal_output stream=stdout|stderr` has independent byte cursors and `terminal close_stdin` sends EOF without removing results. Defaults retain PTY behavior. `terminal_sessions capabilities` reports platform support; Windows pipe mode is explicitly unsupported. Helper-side cwd errors identify missing/non-directory paths. Production verification is recorded above and in `docs/2026-09-profix-upgrade.md`.

- The temporary Mac Beta remains independently installed under `/Users/jamie/Library/Application Support/Executor Beta`. Use its dedicated `stop.py` for Beta lifecycle; the standard lifecycle CLI targets production service labels. See `docs/BETA_MAC.md`.

- The `pro-fix` contracts provide bounded MCP file/terminal byte pages, stable file-write results, explicit input acceptance and nullable command outcomes, desktop-scoped permission diagnostics, independent partial helper-status results, truthful screenshot-export annotations, and explicit non-recursive filesystem operations. Dashboard previews/downloads collect full base64 pages with a 32 MiB browser memory budget and preserve failed-preview save protection. See `docs/MCP_CONTRACTS.md` for the contract details and `docs/2026-09-profix-upgrade.md` for current verification.

- Live desktop is deployed at native source `102a7d1` on signed Mac and Windows, with Dashboard UI `4d128a6` / Worker `9efdc9ee-0f53-442f-a830-a02a5b2e9ef6`. Selectable 15/30 FPS remains available (30 FPS default). A1-US and A1-JP now each run an authenticated, resource-limited coturn service under dedicated instance-only OCI groups (TCP/UDP 5349, UDP 49160–49200); existing DERP/DNS/app services are preserved. Long-term relay credentials are protected outside the repo; each unlocked browser obtains fresh one-hour tickets automatically. Real browser verification covers Windows relay-only video/input/drag/scroll/Chinese text, TCP-only relay with 30 FPS and no lost packets in the sample, Mac relay-only video, and automatic mode selecting host-to-host direct connectivity. Stop cleanup is verified. Windows permits program-specific UDP without source-IP restrictions; Dashboard grants enforce access and TCP/global firewall rules remain unchanged. Recovery/OAuth/IPC credentials are preserved. JP external UDP/TCP and browser JP-only video/Chinese-input tests pass. Both clients retain US endpoints alongside JP and the protected cluster secret is unchanged; no source/binary redeploy was needed for JP. Windows helper reload must wait for the old process to stop before starting again. 60 FPS, actual Mac live pointer/typing, and networks requiring TURN-over-TLS/443 remain outside verified coverage. See `docs/SELF_HOSTED_TURN.md` and `docs/LIVE_DESKTOP.md`.
- The September 2026 review hardens OAuth revocation/expiry and parallel persistence, IPC replay expiry, DCR body validation, JSON-RPC IDs, destructive CLI argument validation, filesystem content validation, and relay cancellation identity retention. Operational audit/relay failures emit credential-free diagnostic codes without discarding already completed tool results or disabling the independent rescue page. Real client validation additionally requires tool execution failures to return standard MCP `isError` content rather than HTTP 500, which clients replace with a generic internal error.
- Terminal completion now waits for final output capture; failed or unconfirmed termination retains the session for retry. Output buffering uses bounded bulk copies, with wraparound/cursor regression tests and microbenchmarks. macOS and Pi Linux terminal runtime tests cover these changes.
- Dashboard polling does not cancel slow output requests, stale reads cannot replace newer selections, malformed results fail visibly, and a failed file preview cannot be saved as an empty file. Transport loss reports an unconfirmed outcome, not a guarantee that the host operation failed. Terminal output uses a bounded, read-only `@xterm/headless` parser for UTF-8/VT stream state, including PowerShell cursor redraw; it never sends terminal replies to the host. Cursor gaps reset the parser with an explicit truncation notice, and rapid session reattachment preserves queued output.
- Service templates escape macOS XML and distinguish systemd executable arguments from whole-path WorkingDirectory values. Unsupported working-directory characters are rejected before installation. Windows uninstall preserves state if rollback fails. Native Windows validation also requires process-wide serialization of OAuth state readers/writers and shell-free npm CLI invocation to avoid file replacement sharing failures and Node deprecation warnings interrupting PowerShell deployment.
- Mac (signed Desktop app), Pi5, and Debian WSL run hash-verified source `b75f0fb`; Windows CTPS runs `db1b7e6` after the physical-pixel input fix. All four passed full doctor checks and repeated status checks; production config and credentials are preserved. Central Dashboard Worker version `e220fc40-b5d0-4f3c-97ce-19a9f73923db` (UI source `315d8a4`) is deployed with unchanged Access/bindings/enrollment settings. Authenticated production in-app-browser verification covers terminal/files, captures, permissions/audit, split UTF-8, and isolated Windows Computer Use click/type/drag/scroll/key events. Reload preserves browser-bound grants. Pi5/WSL headless limits remain explicit, and destructive lifecycle controls were not invoked. See `docs/2026-09-code-review.md` for evidence and limits.
- Computer Use snapshots pointer gestures (including the selected drag button), captures/cancels pointers correctly, rejects releases outside the rendered image, and serializes refresh/control requests. Pending or abandoned requests cannot leave an executable old capture or overwrite a newer view. Execution errors remain unconfirmed outcomes; the panel does not automatically refresh over the error or suggest blindly replaying actions.
- Windows screenshot and mouse subprocesses initialize DPI awareness before reading display bounds or sending input. Both now use physical primary-display pixels; checked cursor movement fails before a click if the requested point cannot be reached. Failure cleanup releases held input, while successful explicit mouse-down retains its intended hold. Actual 125% display-scale validation covers 2560x1440 captures, matching click/drag event coordinates, typing, scrolling, and keyboard delivery.
- Service health uses the authenticated, protocol-marker-validated `executor.health` IPC method, independent of potentially slow OS desktop availability queries. CLI status, local Dashboard reachability, and lifecycle readiness use this lightweight path; actual desktop status and permission checks retain their original semantics. This fixes live false-offline reports without extending timeouts or adding retries.
- Architecture approved in conversation on 2026-08-15.
- Repository initialized on branch `main`.
- Config, private fallback secret store, metadata-only audit store, OAuth 2.1 with CIMD/DCR, authenticated localhost dashboard, Streamable HTTP MCP, and local stdio MCP are implemented with tests.
- Agent, privileged Broker, active-user Desktop helper, persistent terminal/filesystem/desktop tools, lifecycle control, independent Kill Switch, and Cloudflare named-Tunnel setup are implemented.
- Release archives install stable `executor` and `executor-kill` binaries plus persistent Agent, Broker, Dashboard, Desktop, and Executor-owned cloudflared services with manifest-backed rollback. Existing generic cloudflared services are not replaced or stopped except for an ownership-proven one-time migration from an older Executor install; replaced host services are restored and returned to their prior running state.
- macOS, Linux, and Windows amd64/arm64 builds are verified by cross-compilation. Real target runtime validation now covers macOS LaunchDaemons/LaunchAgent, Windows Services/Scheduled Task with ConPTY through the privileged Broker, Raspberry Pi OS systemd, and WSL systemd; Linux X11/Wayland variants beyond the tested Pi and WSL environments remain platform-dependent.
- Live isolated Cloudflare named-Tunnel deployments are validated at `executor-pi5.0ruka.dev`, `executor-mac.0ruka.dev`, `executor-windows.0ruka.dev`, and `executor-wsl.0ruka.dev`. Each publishes valid OAuth metadata and rejects unauthenticated MCP requests. The temporary deployment API token was revoked and API-token files were removed after validation.
- A real macOS arm64 process-level test verified OAuth DCR + PKCE, MCP initialization, owner filesystem and terminal calls, window observation, a 3024x1964 screenshot, immediate Kill quiescing, secret rotation, old-token rejection after Resume, and immediate Dashboard key rollover. The installed LaunchDaemon/LaunchAgent deployment additionally verifies Agent, privileged Broker, Dashboard, active-user Desktop, and Executor-owned cloudflared runtime health.
- Lifecycle CLI and installers share stable default state locations (`/var/lib/executor` on Unix and `%ProgramData%\Executor` on Windows), while preserving `EXECUTOR_STATE_DIR` overrides.
- Metadata-only audit events are now written for remote and stdio tool attempts/outcomes without command text, file content, or output.
- OAuth DCR accepts standard metadata but restricts callback hosts to ChatGPT or loopback, and the consent page displays the requesting client and redirect destination. MCP stdio uses newline-delimited JSON; Streamable HTTP validates Origin and protocol headers.
- OAuth authorization-server metadata currently directs ChatGPT through DCR because repeated real ChatGPT web CIMD callbacks stopped before token exchange. The CIMD resolver remains implemented but is not advertised until that callback path is interoperable.
- ChatGPT CIMD token exchange supports both public-client `none` and signed `private_key_jwt` with trusted-origin JWKS verification. The authorization form keeps a native submit control and adds a CSP-hash-restricted primary-pointer release fallback for Safari/embedded-browser first-click loss while preserving native validation, keyboard submission, and the MCP `resource` parameter.
- OAuth token attempts and outcomes are recorded as metadata-only audit events with the authentication method, grant type, result code, and credential-free reason. Client IDs, authorization codes, assertions, tokens, and recovery keys are never recorded.
- AI-managed deployment and credential-rotation instructions require the agent to return each newly generated recovery key verbatim in the requesting owner's private chat while keeping it out of files, logs, Git, and public collaboration surfaces.
- OAuth state replacement preserves the existing Unix owner and group, so a root-initiated Kill or rotate cannot leave the normal-user Agent unable to read `oauth-state.json`; a real root regression test covers the ownership boundary.
- Resume verifies that Broker, Desktop, and Agent loaded the rotated credentials before starting the tunnel or removing the disabled marker.
- Linux/WSL units install under the standard systemd system/user directories. Darwin release jobs build on macOS with native CoreGraphics desktop input.
- Linux terminal shutdown freezes and terminates the complete `/proc` descendant tree, including background jobs that util-linux `script` places in separate process groups; regression tests run on real Pi5 Linux in addition to macOS.
- Windows terminal sessions use the native ConPTY API with persistent input/output, case-insensitive environment overrides, cwd preservation, live resize, and bounded process-tree teardown through a kill-on-close Job Object. Real Windows amd64 tests verify PowerShell state, LF command input, resize, inherited Ctrl+C recovery, no-parent-console startup, child-process termination, Windows Service execution, and the active-user Desktop Scheduled Task.
- The Windows active-user Desktop Scheduled Task disables the PowerShell default 72-hour execution limit so owner terminals and desktop control remain continuously available. Ordinary MCP tool results include serialized text content alongside structured content for ChatGPT compatibility; explicit screenshot text and image blocks are preserved.
- Safari may leave a completed ChatGPT Work response visually stuck on its first streamed token. Reload the same conversation before diagnosing Executor; the full stored response and tool details can then appear. Continue to use Executor audit entries and host state as independent proof.
- Setup preserves the preferred loopback ports when available and automatically persists free loopback ports when another process owns them. Agent and Dashboard status/doctor probes require the authenticated Executor health marker instead of treating any listener as healthy.
- `executor doctor --full` verifies that the public hostname reaches Executor's DCR endpoint with a deliberately invalid, non-registering request and reports an actionable Cloudflare Bot Fight Mode/WAF diagnosis for HTTP 403 responses.
- Installed services are active and healthy on the Pi5, this Mac, Windows `ctps`, and its Debian WSL instance. macOS service rendering uses the native `wheel` administrator group, and Unix credential rotation preserves the installed secret store owner and mode.
- MCP Computer Use now returns screenshots as image content with metadata-only structured output, enforces single-use capture IDs with global invalidation after any desktop control, validates coordinates and platform capability before input, executes nine-action batches, serializes observe/control in both Agent and Desktop helper, and automatically returns the updated screenshot. Oversized PNGs are re-encoded as same-dimension JPEGs; screenshot bytes and typed text remain outside audit storage.
- macOS and Windows Computer Use capture the primary display for coordinate consistency; macOS Retina output is normalized to display points. Linux X11 supports the complete action surface, while unreliable advanced Wayland mouse actions return explicit unavailable errors.
- Darwin archives package the active-user helper as signed `Executor Desktop.app` (`dev.0ruka.executor.desktop`) and bootstrap installs it under `/Library/Application Support/Executor`; production release builds should set `EXECUTOR_MACOS_SIGN_IDENTITY` to a stable Developer ID Application identity. Screenshot results expose a matching MCP output schema, a complete capture-ID text summary, and image content.
- Deployment documentation now treats clone installs as source-build deployments whose canonical wrapper entrypoints are `scripts/deploy-from-source.sh` on macOS/Linux/WSL and `scripts/deploy-from-source.ps1` on Windows. If a checkout lacks those wrappers, agents should treat the clone as missing a self-contained source-deployment path and either use a release bundle or perform the equivalent build-plus-bootstrap flow explicitly.
- Deployment docs now require agents to call out these prerequisites before first install: Go 1.24+, `cloudflared` 2025.4.0+, admin/root, Python 3 on Unix, and `systemd` on Linux/WSL; Cloudflare API token files on Unix must be regular files with mode `0600`.
- Deployment docs now explicitly state that web ChatGPT cannot self-install Executor before MCP exists, and that each setup attempt must be watched for recovery-key output so the newest printed key can be returned verbatim to the requesting owner and saved by hostname.
- Clone deployment wrappers build only the current host target, bootstrap validates the complete payload and Cloudflare readiness before changing host state or generating credentials, repeated setup no longer claims an empty recovery key, WSL is auto-detected, and Windows desktop startup targets the active console user or an explicit `EXECUTOR_DESKTOP_USER`.
- Unified permission initialization is available through MCP `device_permissions`, `executor permissions`, and authenticated loopback Dashboard APIs. Every path calls the active-user Desktop helper, bootstrap performs a bounded readiness retry before `request-all`, and reports distinguish a request from actual approval. macOS uses the installed helper identity for Screen Recording, Accessibility, and Input Control prompts; Windows verifies the unlocked `Default` input desktop; Linux/WSL verify the live AT-SPI bus, `ydotoold` access, X11 `wmctrl`, WSL kernel identity, and request helper restart after dependency inventory changes.
- Windows bootstrap grants the resolved active Desktop user modification rights only on Executor's `.config.lock` and `.secrets.lock` files, so the limited interactive Scheduled Task can load the protected read-only config and secret store without broadening state-directory access.
- Unified Dashboard clone deployment and packaging are implemented locally: canonical Unix/Windows source entrypoints validate the pinned Dashboard package, protected Cloudflare token file, exact D1/Access ownership, temporary Wrangler Custom Domain config, migrations, Worker secret hash, and versioned retry state without changing per-device MCP hostnames, OAuth, or Tunnels. Host source wrappers optionally enroll through protected temporary copies and verify config/service state without rolling back a healthy install on enrollment failure. The authenticated loopback page is now emergency rescue only; permission management remains in the CLI, MCP, and centralized Dashboard. Release archives carry the complete clean Dashboard source payload, and Windows CI parses and exercises PowerShell ACL/enrollment paths; release publishing is gated on the Ubuntu/Windows Dashboard checks and six cross-platform artifact builds.
- Unified Dashboard deployment now maintains a second, more-specific Cloudflare Access application for exactly `<dashboard-hostname>/api/device/*` with one Executor-owned bypass policy; the general hostname retains exactly one owner-email allow policy, and the Worker remains the final enrollment and signed-device boundary. Upgrades query exact remote D1 migration names/order, bind ownership to current Worker deployment/version IDs, preserve disabled enrollment, expose fresh relay connectivity through the authenticated loopback service, and package only Git-tracked source files. Unix symlink ancestors and Windows reparse points are rejected for credential/state paths; Windows deployment parameters use the documented environment defaults with explicit-parameter precedence.
- Dashboard deployment interruption recovery now binds pending D1 ownership only to a persisted create-response UUID, completes pending Worker identity recovery before enrollment rotation, and holds the protected Cloudflare token value across local preflight without a pathname re-read. Worker secrets never use repository files; first-deploy fallback material stays in the protected external state directory. Enrollment helpers delete a designated temporary source only when its captured native file identity still matches, and relay waits are capped at a conservative 300 seconds on Unix and Windows.
- Relay and protocol hardening now gives lifecycle, Computer Use, and permission-consent requests a bounded 120-second response window while preserving the 10-second limit for ordinary calls. Remote Kill defers stopping the Agent until its encrypted one-time result has been handed to the relay, Streamable HTTP caps request bodies at 80 MiB, returns explicit null IDs on parse errors, and rejects trailing JSON, invalid JSON-RPC versions, and non-object tool arguments; enrollment cleanup preserves any token file that changed after the request began.
- A real Windows recovery confirmed that Microsoft Defender can falsely classify the unsigned Executor binary, quarantine it, and remove the Agent, Broker, Dashboard services and Desktop task while leaving the tunnel running. Recovery must preserve the existing config and secrets, avoid Setup/Kill/Rotate when no credential change is requested, restore only a checksum-verified binary, and use at most an exact-file Defender exclusion for the stable `executor.exe`; never exclude the state directory or secret material. If WSL localhost forwarding has occupied the configured loopback ports while Windows Executor was absent, restore the Windows listeners first and verify that the Windows and WSL public OAuth issuers remain distinct.

## Runtime targets

- macOS: LaunchDaemon for agent/broker, LaunchAgent for desktop helper.
- Windows: Windows Services for agent/broker, active-user startup for desktop helper.
- Linux: systemd services plus active-user desktop helper.
- WSL: Linux terminal/filesystem support; WSLg GUI only. Windows desktop requires the Windows companion.

[Unit]
Description=Executor desktop helper

[Service]
Type=simple
ExecStart={{ .BinaryPath | systemdPath }} desktop --config {{ .ConfigPath | systemdArg }}
Restart=always
RestartSec=5
WorkingDirectory={{ .DataDir | systemdWorkingDirectory }}

[Install]
WantedBy=default.target
